{
    "name": "mikesmid/budgetapp",
    "description": "Mobile-first budget- en kasstroombeheer app (PHP + SQLite).",
    "type": "project",
    "require": {
        "php": ">=8.1",
        "ext-pdo": "*",
        "ext-pdo_sqlite": "*"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
}
